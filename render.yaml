services:
  - type: web
    name: smartpools-chatbot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port=$PORT"
    runtime: python
    region: oregon
    branch: main
    autoDeploy: true